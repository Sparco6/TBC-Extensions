# TBCExtensions developer handoff

**Corrected controlled prototype artifact:** `build-x86-vs18/bin/Release/TBCExtensions.dll`, 248,832 bytes, SHA-256 `B801058E392D0DBD28189FB2C95E8D95A4247659C51A7304F757DF404C257DAC`, PE32 x86 Release, `UNSAFE_LEGACY_PORT=OFF`. Owner generation 1 of the prior artifact safely rejected code 5 because constructor entry preceded publication of the enclosing source-call result. The corrected gate carries exact active caller/resource scope and retains parser result, pointer, path, generation, thread, 3/8/0, 64x64, 5/0, memory and ambiguity checks. Symbolic rejection diagnostics are included. Retest required; runtime transcode remains DISABLED.

**Latest lifetime decision (2026-09-16):** VERIFIED_STATIC device-reset code `0x005AB0A0` preserves descriptors while releasing/replacing their GPU texture, sets recreation state, and marks upload dirty. Retained descriptors can revisit `0x005AABD0`/`0x005AACF0`; callback `0x00454700` rebuilds cleared `resource+0x120` and rereads resource selectors on every upload. Descriptor teardown is `0x005AAB90 → 0x0059C060` (COM release, collection unlink, 0x50-byte free). Therefore a controlled encoding-3 correction cannot be initial-upload-only or restored at descriptor return: representation 1/2 must persist for the full resource reuse lifetime. Prefer persistent, narrowly fail-closed encoding-3/alpha8/0 classification at resource initialization over temporary selector patching. Exact resource cache eviction/destruction remains UNKNOWN; no mutation/build in this task, replacement DISABLED, `UNSAFE_LEGACY_PORT=OFF`.

**Next-task gate:** `0x004560CA` writes raw selector #2 and `0x004560E2` writes selector #1 into the resource after successful parser/mip initialization. For the exact armed `Interface\\MasterWoW\\BLP2Test` control with verified BLP2 3/8/0, valid dimensions/mips and existing 5/0 state, a future developer-only prototype may persistently write 2/1 before descriptor construction and never restore them. Decision: **READY_FOR_CONTROLLED_PROTOTYPE** for that one controlled resource; every mismatch and every other encoding-3 variant must pass through unchanged. No mutation was made in this task.

**Latest test artifact:** `build-x86-vs18/bin/Release/TBCExtensions.dll`, 240,128 bytes, SHA-256 `2B4B40995E106AFB8A7E02EAE74B7BAC13E5257C863DC95A9BE199C781A4E0A9`. It adds read-only, normal-entry descriptor correlation at `0x0059A860` and backend correlation at `0x005AAF40`; exact signatures, controlled state and ambiguity shutdown are mandatory. No selector/source/GPU mutation and no mid-function hook. Owner must test DXT5 then BGRA8 and report GPU CORRELATION, resource/descriptor equality, threads, selectors, dimensions and unchanged visuals. Until then classification is IMPLEMENTED / NOT RUNTIME VERIFIED; replacement DISABLED, `UNSAFE_LEGACY_PORT=OFF`.

**Latest correlation result:** VERIFIED_SAFE_CORRELATION is now statically qualified. Hookable normal entry `0x0059A860` receives controlled resource at argument 8 and returns the exact newly allocated descriptor through argument 10 after storing resource at descriptor `+0x30`. Hookable normal backend entry `0x005AAF40` receives that descriptor and calls both creation and upload with it. The next observation design should use exact descriptor equality plus `+0x30` resource equality, expected dimensions/selectors, one-descriptor-per-generation and ambiguity shutdown. Do not use path-only or last-texture assumptions. No hook/DLL was produced in this task; final D3DFORMAT, LockRect Pitch, descriptor destructor, and mip-source lifetime remain UNKNOWN. Replacement DISABLED; `UNSAFE_LEGACY_PORT=OFF`.

**Current observation-build gate:** Do not deploy a new GPU trace DLL yet. The first and retry creation calls are `0x005AAC71`/`0x005AACC9`; upload LockRect/copy calls are `0x005AAE09`/`0x005AAE99`. A 5-byte direct copy call might support a register-preserving trampoline, but no tested mid-function relocation, graphics-thread-safe installation, or exclusive armed-generation→descriptor correlation exists. Existing hooks remain the only installed observation points. This is NOT READY, not a finding that BGRA8 fails to reach GPU creation. Runtime replacement DISABLED; `UNSAFE_LEGACY_PORT=OFF`.

**Latest engineering gate:** The exact final format-bearing COM call is `0x005AAC71`; LockRect-shaped `0x005AAE09` returns destination Pitch/pBits before `0x005AAE99 → 0x005B1A60`. Their register/stack contracts and retry behavior are documented in [runtime-transform research](TBC_BLP2_RUNTIME_TRANSFORM.md). No mid-call probe was installed because ordinary entry detours cannot safely preserve/correlate these live values. Stock selector pair 1/2 is a coherent VERIFIED_STATIC A8R8G8B8 creation + width×4 callback + unswizzled pitch-aware copy route; it is **not** a controlled encoding-3 runtime result. Decision: NOT READY for controlled 5/0→1/2 modification, and NOT READY for runtime BGRA8→DXT5 transcode until final format/Pitch and backing-source lifetime are established. No DLL/source behavior change; replacement DISABLED, `UNSAFE_LEGACY_PORT=OFF`.

**Newest narrow handoff:** VERIFIED_STATIC `0x005B1A60` dispatches the stock class-1 raw-copy routine `0x005AF7D0` (width×4 bytes per row, pitch-aware, no swizzle) and compressed class-5/7 routine `0x005AF730` (block rows). A hypothetical selector 1/2 A8R8G8B8 route has coherent 64×64 raw geometry (16,384 bytes); the controlled BGRA8 5/0 route instead has DXT1-shaped 2,048-byte copy geometry. Final per-request D3DFORMAT, locked pitch, exact first corruption instruction, and source owner are UNKNOWN. The `0x005AABD0` function-entry detour was qualified but **not installed** because it cannot directly observe its internal COM format argument or LockRect pitch. Neither native extension nor runtime transcode is approved; replacement DISABLED, `UNSAFE_LEGACY_PORT=OFF`.

**Latest research handoff:** The descriptor-to-D3D backend is now statically traced: `0x0059A860` creates a 0x50-byte client descriptor; D3D context slot 0 `0x005AAF40` invokes `0x005AABD0` texture creation and `0x005AACF0` mip upload. Initial selector 5 maps to DXT1 and selector 7 to DXT5 at the actual CreateTexture-format table; selector 1 maps to stock A8R8G8B8. The BGRA8 5/0 callback reports 128 bytes per 64-pixel DXT1 block row although its raw BGRA8 logical row is 256 bytes; DXT5 7/7 reports a matching 256-byte DXT5 block row. This is a VERIFIED_STATIC format/pitch interpretation mismatch, consistent with but not a per-request runtime proof of the corruption mechanism. Creation fallback remap, locked destination pitch, exact copied bytes and source backing lifetime remain UNKNOWN. No selectors, bytes, hooks, or GPU objects were changed. Native extension and runtime transcode are both unapproved; replacement DISABLED, `UNSAFE_LEGACY_PORT=OFF`. Next action: a narrowly gated read-only observation of actual per-request backend format/remap and upload pitch, only after validating the proposed probe contract in [runtime-transform research](TBC_BLP2_RUNTIME_TRANSFORM.md).

**Current BLP handoff checkpoint:** Owner runtime traces establish source/parser result 1 for both controlled assets: DXT5 enc2 alpha8/7 selector pair 7/7 correct, BGRA8 enc3 alpha8/0 pair 5/0 visible but corrupted. Immediate TRACE is an early same-generation snapshot; Native Trace Status only displays accumulated state. The static construction vtable dispatch is now resolved to common wrapper constructor `0x0059A860` via `0x005AAAD0` or direct slot. Do **not** treat this as the D3D9 `CreateTexture` target. Format mapping, upload pitch/copy, relocated mip-table lifetime and first causative corruption point remain UNKNOWN. No new hook/build; runtime replacement DISABLED and `UNSAFE_LEGACY_PORT=OFF`. Next narrow task: trace the descriptor object's first consumer through actual device creation and mip upload before comparing native extension with source transcode.

## New Developer — Start Here

Target: custom 32-bit World of Warcraft TBC 2.4.3 build 8606. The existing `TBCExtensions.dll` is the foundation; do not design a second loader or DLL. Read [architecture](ARCHITECTURE.md), [evidence and address map](TBC_8606_ADDRESS_MAP.md), [BLP research](BLP_TEXTURE_RESEARCH.md), and [build/test procedure](BUILD_AND_INSTALL.md) first. `TBCExtensions 0.1.7-blp2-native-correlation-research` reports native API **7** and profile `MASTERWOW_CUSTOM_8606` when its fingerprint passes (`TBCExtensions/src/Client/CustomLua.cpp`).

What works: the custom executable loader and safe Lua bridge have owner-reported runtime success; the Toolkit can inspect native status; DLL-owned Custom DBC lookup exists; stock Palette/DXT1/DXT3/DXT5 controls and the separate offline BGRA8→DXT5 file rendered in the owner's 8606 client. Owner-performed controlled traces have now correlated **both** DXT5 and original BGRA8 from `0x00457FC0` through `0x00455EF0` to the verified parser `0x005B1BA0`. DXT5 is BLP2 encoding 2, alpha 8/7, 64×64 and renders correct quadrants; original BGRA8 is BLP2 encoding 3, alpha 8/0, 64×64 and produces visible but corrupted pixels. Correct stock BGRA8 decoding is **not** verified. Some repeated requests show a matching resource but no loader/parser hit; cache or resource reuse is a hypothesis, not proven for those individual traces. Do **not** enable `UNSAFE_LEGACY_PORT`, replace BLP bytes, claim correct BGRA8 runtime compatibility, or start retail M2/SKIN. See [BLP research](BLP_TEXTURE_RESEARCH.md) and [RE state](REVERSE_ENGINEERING_STATUS.md).

The x86 Release observation-v2 DLL was compiled and **owner-tested**. It uses the same three research hooks. Both different requests returned `0x27E8AC08` from `0x00457FC0`; the `0x00455EF0` pointer-equality candidate count was zero for both. Neither pointer equality nor a repeated address establishes per-texture identity: the preview is cleared between requests and the setter releases its prior refcounted resource. Exact DLL hash and installation are in [build/install](BUILD_AND_INSTALL.md).

**Current observation:** The same three pass-through detours use the inline resource name at `+0x0C` rather than raw pointer equality. Owner tests established path-correlated source/parser observations for both controls. The former Toolkit `64/0` and `64/7` labels were misleading: resource `+0x144` is width (64), while `+0x118` is one internal selector (0 or 7). The updated status reads the other selector from resource `+0x14C` and prints dimensions separately; GPU format remains UNKNOWN. `Native Trace Status` displays state only; TRACE buttons arm and issue one controlled request. Cache state for each request remains UNKNOWN. No hook address, source bytes, descriptor fields, cache behavior, or stock return value changed. See [RE state](REVERSE_ENGINEERING_STATUS.md) and [manual procedure](TESTING_GUIDE.md).

**Latest owner-controlled comparison:** Both formats transition from immediate TRACE request/name HIT with SOURCE/PARSER UNKNOWN to **same-generation** PATH CANDIDATE/PATH-CALL HIT shown later by display-only Native Trace Status. That is not another request or cache-hit evidence. The latest DLL made both selector lines legible: DXT5 generation 4 had BLP2 enc2 alpha8/7 64×64, source/parser result1, selectors **7/7**, correct quadrants; BGRA8 generation 5 had BLP2 enc3 alpha8/0 64×64, source/parser result1, selectors **5/0**, visible corrupted stripes. Both used thread 40336 and source `0x0091309C`; resource names correctly matched their distinct paths. No new hook or compatibility fix is present. The inspected `0x00455EF0 → 0x005B3210` call supplies fifth argument 1, routing **both** formats through pointer relocation at `0x005B3327`; the raw-copy branch of `0x005B2F70` is not proven on this call. See [runtime-transform RE note](TBC_BLP2_RUNTIME_TRANSFORM.md).

Evidence labels throughout this package: **RUNTIME VERIFIED** = owner observed in 8606; **STATIC VERIFIED** = disassembly/signature evidence, not necessarily the controlled path; **IMPLEMENTED / NOT RUNTIME TESTED** = source/build exists; **EXPERIMENTAL** = research-only hook; **CANDIDATE** = plausible but unproven; **UNKNOWN** = insufficient evidence; **DEPRECATED / SUPERSEDED** = older conclusion replaced. An address being mapped, readable, or visible in Toolkit is not a runtime call hit.

## Project status matrix

| Subsystem | Implemented | Static verified | Runtime verified | Production ready | Evidence/limit |
|---|---|---|---|---|---|
| Executable loader | Yes | Yes | Owner reported | Existing baseline | Nine-byte-region manifest; preserve custom exe |
| Build fingerprint | Yes | Yes | Valid profile reported | Gate only | Does not validate every legacy address |
| Lua bridge / callback validator | Yes | Yes | Owner reported | Safe research subset | Exact callback allowlist; legacy port OFF |
| Developer Toolkit/address inspector | Yes | Yes | Screenshots | Diagnostic | Inspector never calls addresses |
| DLL-owned Custom DBC | Yes | Yes | Prior API tests reported | Read-only lookup | No stock creature-display integration |
| Stock Palette/DXT1/3/5 | Test assets | N/A | Owner PASS | Stock route | Test archive must be available |
| Independent BLP parser | Yes | Yes | Toolkit BGRA8 parse OK | Offline/inspection | Does not alter stock texture loader |
| Offline BGRA8→DXT5 | Yes | Yes | Owner visual PASS | Controlled proof | Separate file only |
| Native BLP observation | Yes | Yes | Both controls request/source/parser path-associated HIT | No | Research-only; path candidate is not unique causal proof |
| Correct stock BGRA8 rendering | No | No | Owner saw corrupted output | No | Visible is not correct decoding; replacement DISABLED |
| Real WotLK BLP | No | No | No | No | Deferred |
| FileDataID resolver / modern M2 / SKIN / D3D9 retail renderer | No | No | No | No | AoREngine is reference, not TBC implementation |
| Animations / attachments / particles / ribbons | No | No | No | No | Future port phases |
| CreatureDisplay / item / character retail integration | No | No | No | No | Custom rows alone do not connect to stock pipeline |

Detailed topics: [architecture](ARCHITECTURE.md), [RE state](REVERSE_ENGINEERING_STATUS.md), [address registry](TBC_8606_ADDRESS_MAP.md), [texture history](BLP_TEXTURE_RESEARCH.md), [manual tests](TESTING_GUIDE.md), [build/install](BUILD_AND_INSTALL.md), [roadmap](ROADMAP.md), [issues](KNOWN_ISSUES.md), and [research decisions](CHANGELOG_RESEARCH.md). Older `Docs/ADDRESS_STATUS.md` reports `0/44` and TODO Lua addresses; it is **DEPRECATED / SUPERSEDED** for the safe bridge by `Offsets/ClientOffsets_8606.hpp` and this handoff. Legacy modules still contain real TODOs.
**Latest owner result and test artifact:** The owner VERIFIED_RUNTIME the prior descriptor/backend observation for both controls: DXT5 resource `0x1BF69C08` → descriptor `0x2294C188`, BGRA8 resource `0x1BA7CE08` → descriptor `0x268B6808`, exact `descriptor+0x30` equality, constructor/backend thread 34840, no ambiguity, correct DXT5 and striped BGRA8. The new read-only DLL is `build-x86-vs18/bin/Release/TBCExtensions.dll`, 244,224 bytes, SHA-256 `81D2C45D9CA6DCCFE6F9577B9E31CCFD18A591D0BF0209B98DDF6211EC0AE562`. It adds exact-signature-gated normal-entry probes at `0x005AABD0`, `0x005AACF0`, `0x005B1A60`. Owner runtime testing is required; replacement DISABLED, `UNSAFE_LEGACY_PORT=OFF`.
**Latest owner GPU result:** The read-only build is runtime-verified. DXT5 7/7 stayed accepted 7/DXT5 with source and destination Pitch 256 and class 7/7 (4,096 derived bytes), correct visual. BGRA8 5/0 stayed accepted 5/DXT1 with Pitch 128 and class 5/5 (2,048 derived bytes from raw 16,384-byte content), striped visual. This is now VERIFIED_RUNTIME. Pair 1/2 is statically coherent, but a safe prototype is NOT_READY: descriptor-only changes do not alter callback Pitch; resource changes must precede construction and remain consistent with descriptor class through any later cached/device-reset upload. Safe restoration/resource end-of-life is UNKNOWN. No code or DLL was changed for this decision; use the prior observation DLL only for research.

**Latest controlled prototype result:** Owner testing proved the exact resource mutation 5/0→1/2 applied with all gates and exact descriptor correlation. It also proved that constructor-time resource mutation is too late for descriptor classification: CREATE remained selector 5/DXT1, COPY remained 5/5, source Pitch became 256 from the live resource, and destination Pitch remained 128. The visual stayed corrupted. Preserve labels `CONTROLLED_RESOURCE_MUTATION_VERIFIED_RUNTIME` and `FULL_NATIVE_1_2_PATH_NOT_YET_ACHIEVED`.

**Next engineering boundary:** `0x00455EF0` pushes its locally snapshotted selector #1 and raw selector #2 to `0x00454F40` independently of the resource pointer. `0x00454F40` is the first verified normal entry where exact resource and both pretranslation selector values coexist; it translates raw 0→class 5 and starts the request/descriptor chain. A next-task prototype may move the exact fail-closed controlled correction there, requiring both resource and arguments to be 5/0 before forwarding resource/arguments 1/2. Do not patch descriptor fields, global tables, or D3D calls. No DLL was rebuilt for this static follow-up.

**New test artifact:** The controlled correction is now implemented at normal entry `0x00454F40`. It keeps the existing PROTO BGRA8 arm, requires exact resource and argument 5/0 together with every prior parser/path/thread/metadata gate, then persists resource 1/2 and forwards only selector arguments 1/2. New status lines expose classifier HIT and incoming/forwarded values. Stock TRACE BGRA8 and TRACE DXT5 never arm this mutation. Classification is CONTROLLED_CLASSIFICATION_1_2_PROTOTYPE / RUNTIME_VERIFICATION_PENDING; do not call it working until owner GPU trace and pixels confirm it.

**Owner verification complete:** The exact controlled native path is now VERIFIED_RUNTIME: 5/0→1/2 at `0x00454F40`, descriptor/copy 1/1, A8R8G8B8, 64 raw rows/16,384 derived bytes, correct quadrants. DXT5 remained correct and unarmed BGRA remained outside the classifier. Runtime BGRA→DXT5 is unnecessary for this verified 3/8/0 case.

**Current development boundary:** Production-global support is still disabled. The DLL contains a compile-time, OFF-by-default developer allowlist with only `Interface\\MasterWoW\\BLP2Test.blp` (64×64, one mip). The offline `blp_classify.exe` tool identifies structural candidates but does not modify the allowlist. Before adding an asset, require exact 3/8/0 raw mip sizes and then run a separately selectable fresh-client trace including all mip details. No real WotLK/custom corpus was found locally.
