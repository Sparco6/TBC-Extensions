# RE state and evidence boundaries

**IMPLEMENTED / OWNER RUNTIME TEST PENDING:** Normal-entry hooks at `0x0059A860` and `0x005AAF40` convert the static identity chain into a fail-closed runtime experiment. They snapshot only when generation, bounded path, dimensions, selectors, resource, descriptor and descriptor `+0x30` all agree; multiple descriptors or re-entry mark AMBIGUOUS. Original calls and returns are preserved. Internal COM/LockRect/copy sites remain untouched, so GPU format/Pitch/class remain UNKNOWN. Build is x86 Release with unsafe legacy port OFF.

**VERIFIED_STATIC identity chain:** `0x0059A860` is a stable function entry carrying both resource (argument 8) and output descriptor location (argument 10); construction writes resource to descriptor `+0x30`. `0x005AAF40` is a stable `__thiscall`-shaped backend entry carrying that descriptor at `[ebp+8]` and synchronously calls creation/upload with it. A future trace can record one descriptor per armed generation, revalidate exact descriptor/resource equality at backend entry, and mark multiple descriptors or changed relationships ambiguous. Classification: VERIFIED_SAFE_CORRELATION, but GPU format/Pitch observation and descriptor destruction remain unimplemented/UNKNOWN.

**GPU observation build: NOT READY.** Creation has two distinct call sites (`0x005AAC71`, retry `0x005AACC9`); LockRect/copy windows are `0x005AAE09`/`0x005AAE99`. Each lies inside a graphics function, with live COM stack/register/EFLAGS state. Current research hooks correlate a resource path but do not uniquely bind the 0x50-byte descriptor to one armed generation. No verified register-preserving mid-function patch mechanism or active-thread installation proof exists in the project. No hook/code/DLL change; final controlled format/Pitch/class still UNKNOWN.

**Current GPU evidence gap:** VERIFIED_STATIC `0x005AAC71` is the final format-bearing CreateTexture-shaped call; `0x005AAE09` is the LockRect-shaped call and `0x005AAE99` feeds its Pitch/pBits to `0x005B1A60`. Their exact live contracts are in `TBC_BLP2_RUNTIME_TRANSFORM.md`. These are mid-function calls without hook prologues; the copy dispatcher has no controlled resource identity argument. No observation detour was installed. Final per-request format, destination Pitch, copied result and source owner remain UNKNOWN. Candidate 1/2 is statically coherent but not ready for selector modification; runtime replacement DISABLED.

**Latest VERIFIED_STATIC upload-copy boundary:** `0x005B1A60` sends internal pair 1/1 to `0x005AF7D0`, which copies 32-bit rows with separate source/destination pitch steps and no byte permutation. Pairs 5/5 and 7/7 go to `0x005AF730`, which copies compressed block rows. At 64×64 the static full-mip byte counts are 16,384 / 2,048 / 4,096 respectively. Controlled BGRA8 5/0 yields DXT1-shaped class 5/5, not the plausible A8R8G8B8 pair 1/2. This is a VERIFIED_STATIC representation mismatch, not a measured final runtime GPU format or exact corruption instruction. Destination LockRect pitch and source backing lifetime remain UNKNOWN; no new hook was installed.

**BLP D3D path (VERIFIED_STATIC, not a compatibility fix):** Client descriptor `0x0059A2F0` is 0x50 bytes without a vtable; D3D context vtable `0x008BFEA0` slot 0 leads to `0x005AAF40`, then creation at `0x005AABD0` and upload at `0x005AACF0`. The creation path passes table-derived D3DFORMAT to a CreateTexture-contract COM call; initial selector 5 maps DXT1, 7 maps DXT5, and 1 maps A8R8G8B8. The mip callback uses resource `+0x120` pointers and computes DXT-style block pitch for controlled 5/0 and 7/7 pairs. BGRA8's raw payload versus DXT1 descriptor is the first proven static representation mismatch. Dynamic remap, upload byte count, source ownership and controlled runtime GPU format remain UNKNOWN; replacement DISABLED.

**BLP construction status — VERIFIED_STATIC:** `0x00596CB2` dispatches through graphics-context vtable `+0xCC`. The two statically identified context vtables resolve to `0x005AAAD0` (jumping to `0x0059A860`) and directly to `0x0059A860`. The latter calls `0x0059A2F0` and `0x00598D60` to construct a client descriptor object; its ten-stack-argument contract is established by `ret 0x28`. The eventual D3D9 creation call, format translation, mip-table ownership and upload contract are UNKNOWN. Runtime BGRA8 replacement stays DISABLED.

The 8606 safe Lua bridge uses the central offsets in `TBCExtensions/src/Offsets/ClientOffsets_8606.hpp`; image and ten critical Lua byte checks are in `Client/CustomLua.cpp`. `Main.cpp` verifies the registration and callback-validator prologues. Those addresses are qualified for their safe bridge use, not proof of every legacy API in `CustomLua.cpp`. Other source still has WotLK-derived zero placeholders; see [known issues](KNOWN_ISSUES.md).

Texture path evidence has one runtime-confirmed controlled boundary: `0x00457FC0`. The registration `SetTexture`→`0x00434BE0`, setter `0x0042F920`, call toward `0x00457FC0`, alternate branch `0x004576E0`→`0x00455EF0`→`0x005B1BA0` are disassembly-based **STATIC VERIFIED links**, not a fully **RUNTIME VERIFIED chain**. The owner-tested resource-identity build reported one matched non-null request per asset on thread 30304, both with numerical return pointer `0x27E8AC08`; the deep pointer-equality candidate count was zero and deep generation-correlated source/parser hits were zero. Global totals rose, but cannot be assigned to either request. Deferred loading, alternate branches, cache hits, unrelated requests, and correlation loss remain possible. Fresh-client visual testing established that original BGRA8 produces visible **incorrect** output while offline DXT5 renders correct quadrants. There is no proven clean-failure instruction; the first controlled post-request divergence is **UNKNOWN**. The 0.1.6 `0x005B3170` filtered `NO HIT` did not prove parser miss.

Safety restriction: any new detour needs independent calling-contract and exact prologue/section qualification, fingerprint first, original called unchanged, bounded logging, and no arbitrary pointer dereference. Do **not** use the repeated raw return address as a unique texture key. `0x0042FA15` statically stores the `0x00457FC0` result at Texture `+0xC4`, after releasing the old value through `0x004271A0`; Clear/nil follows the same release/zero path. `0x004271A0` decrements resource refcount `[object+4]` and invokes its vtable destructor at zero; `0x004271D0` adds a reference. Sequential Clear→request tests can therefore reuse an allocation address. In the inspected `0x004576E0` loader branch, the 0x164-byte constructed resource stores the pathname at `+0x0C`, and `0x00453B80` compares that stored path for the setter's same-resource shortcut. These are **STATIC VERIFIED branch relationships**, not a controlled object snapshot or proof that either test used this loader branch.

**Observation-v2 owner result:** The x86 Release DLL was compiled and owner-tested. It did not dereference or mutate the pointer. Equality against ECX at `0x00455EF0` gave zero candidate calls for both assets. This does **not** establish no loader/parser use; the static route, branch/timing, actual object identity, and cache status remain unresolved. No broader observation hook is authorized by this result.

**Static cache and descriptor leads:** `0x00457FC0` makes a local path copy, strips an extension in that copy through `0x00453AC0`, and calls `0x004566C0` before either inspected loader branch. `0x004566C0` derives a key through `0x00644F70`, duplicates the path, and searches `0x00456560`; the inspected entry comparison uses a 4-byte key at entry `+0x00` **and** a string at `+0x14`. A hit takes an addref on the resource at entry `+0x1C` and returns it at `0x00458136`, bypassing the loader calls in this invocation. On an inspected successful miss, `0x00457D20` writes the key at entry `+0x00`, path/name at `+0x14`, and resource pointer at `+0x1C`; this is a concrete cache insertion branch. Cache hit/miss for the two controlled requests, negative entries, and exact path normalization are UNKNOWN. In `0x00455EF0`, parsed header byte `+0x0E` is passed to the jump table at `0x00454820`; for the known test metadata, BGRA8 uses table entry 0 and DXT5 uses entry 7. Entry 0 selects a format-index pair conditionally from `(5,0)`, `(4,5)`, or `(3,3)`; entry 7 selects `(7,7)` when its device feature is present or `(2,4)` otherwise. The meanings of both index families and eventual D3D9 format are **UNKNOWN**, but this is a high-value candidate first descriptor/format difference. `0x005B3210` receives one selected index and can relocate mip pointers; static proof does not establish either controlled request reached it.

`0x0091309C` is a static read destination on the alternative UI resource route, **not** an object of established capacity or lifetime. `0x00D43118/+0x20` belongs to the earlier separate route and can be reused/reallocated. Native parser +0x498 retains source bytes into mip access. Neither route yet supplies a safe replacement-buffer lifetime model for the controlled Interface texture. See `Docs/TBC_BLP2_RUNTIME_TRANSFORM.md` for the instruction-level ownership graph; do not infer a complete graph for the Interface route.

**Observation design basis (partly implemented below):** reuse the existing `0x00457FC0` and `0x00455EF0` pass-through detours. Match only a structurally validated, bounded resource name at `+0x0C`, not the numerical pointer. On a matched source-call candidate, observe the original return and post-call fields `+0x118`/`+0x144` read-only. A path match is not unique causal ownership, and unsafe reads remain UNKNOWN. `0x00454820` is a jump-table dispatcher without a normal prologue and is **not** a direct hook entry.

**Implemented observation revision (x86 compiled; owner runtime test pending):** `NativeBLPCorrelation.cpp` implements only the guarded, bounded part of that proposal at the existing detours. Static `0x004577FA` copies a path into the inline 0x104-byte resource field at `+0x0C`; `0x00453B80` reads the same field for the same-resource comparison. It is **not a separate string-pointer member**. The matched request detour validates the returned pointer's readable field, readable vtable and executable destructor, then scans at most 0x104 printable bytes for a NUL; invalid/missing data displays UNKNOWN. `0x00455EF0` applies the same structural check to ECX and labels a matching controlled name **PATH CANDIDATE**, not exact request ownership. Its direct parser call can carry that thread-local path-candidate label only when the parser source equals the caller EDX source: static `0x00455F1D` preserves EDX and `0x00455F3B` passes it to the parser. After a successful original source call, guarded read-only observation takes selected indices from resource `+0x144`/`+0x118`; header alpha depth/encoding are taken only from a verified BLP2 magic and readable header. No pointer-equality correlation is used, and no extra hook was created. A later load of the same path could produce a candidate without being causally tied to the earlier request; controlled runtime parser convergence remains UNKNOWN until owner trace.

**2026-09-16 evidence correction:** Owner tests subsequently showed controlled path-correlated `0x00455EF0` PATH CANDIDATE and nested `0x005B1BA0` PATH-CALL HIT for **both** DXT5 (BLP2 encoding 2, alpha 8/7, 64×64, correct visual quadrants) and original BGRA8 (BLP2 encoding 3, alpha 8/0, 64×64, visible corrupted pixels). The prior “owner runtime pending/parser convergence UNKNOWN” text above is historical. Other generations showed request/resource name HIT without deep correlation; cache/reuse is the leading hypothesis but individual cache status is UNKNOWN. `Native Trace Status` displays state only; each TRACE button arms and issues one request. `+0x144` is resource width, `+0x148` height, `+0x14C` first internal selector, and `+0x118` second selector. Former `64/0` and `64/7` were width/second-selector, **not format-index pairs**. New read-only status separates them. The final D3D9 format and BGRA8 corruption mechanism remain UNKNOWN. No new hook, byte replacement, or descriptor mutation was added.

**Latest same-generation timing and selector mapping:** Owner first/second tests for both assets show immediate TRACE-button request/name HIT with source/parser NO HIT, then display-only Native Trace Status on the **same generation** shows PATH CANDIDATE/PATH-CALL HIT. This is not a second load; cause of later completion is UNKNOWN. BGRA8 first selector 5 and DXT5 first selector 7 are owner-visible; selector #2 was clipped. Static `0x0045615A` passes first selector to `0x00454F40` as its third argument (construction descriptor field), and second selector as seventh argument (indexes `0x00912E3C`, where PE words include entry 0→5 and 7→7). `0x005B2F70` derives mip source from parser `+0x498` plus offset and branches by BLP encoding; encoding 3 has a raw-copy branch at `0x005B3006`, while encoding 2 branches through selector-dependent processing at `0x005B3027`. Exact D3DFORMAT, pitch and upload target remain UNKNOWN. The status-only revision uses separate dimension/selector lines and adds no hook.

**Latest owner runtime 7/7 versus 5/0:** The new unclipped status confirmed DXT5 enc2 alpha8/7 selectors **7/7** and correct pixels, versus BGRA8 enc3 alpha8/0 selectors **5/0** and corrupted stripes; both source/parser result1 on thread 40336. The earlier clipped-selector description is superseded. Caller `0x00456055` passes fifth argument 1 to `0x005B3210`; with these selector/encoding pairs both take `0x005B3327`. That path relocates mip offsets against parser `+0x498` into absolute source pointers, stores them in a third-argument table, clears `+0x498`, and returns without payload copy. `0x005B2F70` remains a real generic mip consumer but is **not reached by this inspected call**. The table's lifetime, actual D3DFORMAT/pitch/upload, and first semantic incompatibility remain UNKNOWN. No new detour, texture mutation, or runtime replacement is justified.
**Current runtime checkpoint:** Owner tests VERIFIED_RUNTIME exact resource→descriptor→backend equality for controlled DXT5 7/7 and BGRA8 5/0, with no ambiguity and unchanged visuals. The new x86 observation DLL adds fail-closed normal-entry tracing at creation `0x005AABD0`, upload `0x005AACF0`, and copy dispatcher `0x005B1A60`. It can snapshot the accepted post-remap selector/format, texture pointer, LockRect-returned destination Pitch/pBits as passed to copy, source pitch/pointer, copy classes and geometry. It cannot distinguish first/retry attempts or HRESULT; those remain UNKNOWN. No selector, source, D3D argument or return value is changed.
**GPU mismatch promoted to VERIFIED_RUNTIME:** DXT5 7/7 is accepted as DXT5 and uploads 16 block rows with runtime Pitch 256/256 (4,096 derived bytes), producing the correct image. Encoding-3 BGRA8 5/0 is accepted as DXT1 and uploads 16 block rows with runtime Pitch 128/128 (2,048 derived bytes), producing stripes. Exact controlled descriptor correlation held throughout. Pair 1/2 remains a coherent VERIFIED_STATIC A8R8G8B8 width×4 route, but mutation is NOT_READY because the resource selectors drive the mip callback while descriptor fields drive creation/copy; both must remain coherent across unknown cache/device re-upload lifetime.

**Descriptor/device-reset lifetime promoted to VERIFIED_STATIC:** `0x005AB0A0` walks retained descriptors, releases/replaces GPU texture `+0x38`, sets `+0x4B` for recreation and dirties upload state. `0x005AAF40`, `0x005A48B0`, and `0x005AAFA0` can invoke `0x005AABD0` again for a retained descriptor; `0x005AAF40` can invoke `0x005AACF0` again when dirty. Callback `0x00454700` can reconstruct cleared mip pointers and rereads resource selectors on every upload. Teardown is `0x005AAB90 → 0x0059C060`, releasing the COM texture, unlinking, and freeing the descriptor. Result: temporary 5/0→1/2→5/0 mutation is unsafe; persistent per-resource encoding-3 classification is required. Exact resource/cache destructor remains UNKNOWN.

**Prototype decision:** `0x004560CA/+0x004560E2` are the verified persistent resource selector stores after parser success. The next task may implement an OFF-by-default, exact-generation/path, exact BLP2 3/8/0 and 5/0-gated persistent 1/2 classification for the single controlled asset. Status: **READY_FOR_CONTROLLED_PROTOTYPE**. No global encoding-3 behavior is approved.

**Implementation checkpoint:** The controlled prototype is implemented at normal entry `0x0059A860`, before original descriptor construction. It is separately armed and writes only `resource+0x118=2` and `resource+0x14C=1` after all exact gates pass. It never restores selectors, allocates no buffer and changes no D3D state. Status remains CONTROLLED_PROTOTYPE / OWNER RUNTIME TEST REQUIRED; it must not be promoted to VERIFIED_RUNTIME from build success.

**Prototype rejection diagnosis:** Owner generation 1 rejected safely with no mutation. Numeric code 5 combined parser/correlation readiness with `g_call.result==1`; the latter is unavailable at nested constructor entry because `ObserveCaller` commits it only after native `0x00455EF0` returns. The revised state machine proves the caller is active using exact thread-local caller-resource scope and still requires parser result 1. Constructor NO HIT in that trace meant attribution was deliberately stopped after rejection, not that the native constructor failed to execute.

**Latest prototype result — VERIFIED_RUNTIME resource mutation, incomplete native route:** The exact controlled resource changed from 5/0 to 1/2 and remained exactly correlated through descriptor/backend. Creation still used selector 5/DXT1 and upload class 5/5; callback source Pitch became 256 while locked destination Pitch remained 128. This exposes two independent selector consumers and explains the new corrupt pattern. Full native 1/2 is not runtime-achieved.

**Address-level cause — VERIFIED_STATIC:** `0x0045613D..0x0045615A` passes local selector #1 and raw selector #2 by value to `0x00454F40`; raw 0 is translated to class 5 before `0x0059A860`. Descriptor fields `+0x24/+0x28` therefore remain 5/5 despite the late resource write. Normal `__cdecl` entry `0x00454F40`, signature `55 8B EC 83 EC 4C 56 57`, is the first verified normal boundary carrying exact resource plus both selector values before translation. It is the next controlled-prototype candidate, not yet hooked.

**Current implementation — owner runtime pending:** The candidate is now an exact-signature-gated normal-entry hook. For only the separately armed exact 64×64 BLP2 3/8/0 resource, it requires resource and incoming arguments both 5/0, persists resource 1/2, and forwards argument 1/2 into stock code. It records original/forwarded values and rejects mismatched arguments, invalid mip metadata, or re-entry without writes. Expected downstream descriptor/GPU/copy state is 1/1 → A8R8G8B8 → pitch-aware 32-bit rows. This expectation is not VERIFIED_RUNTIME.

**Runtime status supersession:** The expected chain is now owner VERIFIED_RUNTIME for the exact control: classifier 5/0→1/2, descriptor 1/1, A8R8G8B8, class 1/1, Pitch 256/256, 64 rows, 16,384 derived bytes, correct visual. DXT5 regression and unarmed-stock isolation passed. Label: `CONTROLLED_NATIVE_BGRA8_1_2_VERIFIED_RUNTIME`.

**Generalization remains gated:** Build 8606 statically supports power-of-two dimensions below 1024 and class-1 row copies across actual mip dimensions, but only 64×64 single-mip has runtime proof. The DLL now has a one-entry developer allowlist plus exact raw-mip-layout validation and bounded 16-mip telemetry. Offline candidates do not become runtime-compatible automatically.
