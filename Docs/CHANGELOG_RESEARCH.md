# Research decisions and superseded findings

Chronology below follows source, package folders, dated patch baseline and owner manual reports; it is **not** a Git commit chronology. Available root Git status/diffs do not establish a reliable complete version history for these research packages. Do not invent dates/commit IDs for undated results.

1. Existing TBCExtensions/custom-loader DLL remains the foundation. Keep the user's modified Wow.exe; patcher validates nine loader ranges and backs up before applying (`Patcher/SafeMain.cpp`, dated `Docs/CUSTOM_WOW_PATCH_BASELINE.md`). The safe bridge fingerprints 8606 before hooks; no WotLK address transplant.
2. `UNSAFE_LEGACY_PORT` defaults OFF. Native Custom DBC was made a DLL-owned WDBC inspector instead of replacing stock TBC DBC storage (`CDBCMgr/NativeDBC.cpp`, `WdbcTable.hpp`). Creature rows are not stock renderer integration.
3. Texture compatibility precedes full retail M2: preserve stock route for known-compatible BLP, investigate compatibility only where a format genuinely fails. Synthetic Palette/DXT controls passed in owner client; original BGRA8 independently parsed but earlier stock preview failed cleanly.
4. Offline BGRA8→DXT5 separate copy was generated/reparsed and owner-rendered correct quadrants. Decision: do not mistake offline success for runtime byte replacement or add a D3D9 texture manager.
5. Native parser +0x498 retains source. `0x005B3170` path uses reused global bytes; no temporary-vector hook. 0.1.6 filtered controlled request gave NO HIT, so trace downward from native Texture method rather than assume this BLP caller is the UI route.
6. The 0.1.6 blank Palette/DXT/offline incident was an open/locked patch/archive. Suspected browser/format regression **DEPRECATED / SUPERSEDED** after restart/control PASS.
7. Static alternate resource route `0x00434BE0`→`0x0042F920`→`0x00457FC0` and possible later parser route was documented; no entire-chain runtime claim. 0.1.7 observation-only hooks were compiled with legacy port OFF and transform disabled.
8. Initial two-button 0.1.7 trace: both requests **RUNTIME VERIFIED** at `0x00457FC0`; no deep generation match despite rising totals. `return 1` is a non-null boolean. The then-ambiguous BGRA8 visual finding is **SUPERSEDED** by fresh-client visual control.
9. Owner-tested observation-v2 revision reuses the same three hooks. BGRA8 and offline DXT5 each returned numerical pointer `0x27E8AC08` on thread 30304; candidate pointer-equal source-caller ECX count was zero for both. The fresh original BGRA8 rendered a distinct corrupted pattern; offline DXT5 rendered correct quadrants. Static setter RE shows a refcounted resource stored at Texture `+0xC4` and released by Clear, so the repeated numerical address is not a unique identity proof. Next: static resource/path/cache and post-parse format-index investigation, without broader hooks or replacement. Real WotLK assets remain deferred.
