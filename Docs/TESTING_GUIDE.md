# Manual testing and evidence recording

## First controlled descriptor/backend correlation test

Install only `build-x86-vs18/bin/Release/TBCExtensions.dll` beside the same custom build-8606 `Wow.exe`, keeping a recoverable prior DLL. Confirm SHA-256 `2B4B40995E106AFB8A7E02EAE74B7BAC13E5257C863DC95A9BE199C781A4E0A9`. Do not replace test BLP files or rerun the executable patcher; close any open patch/archive before launch.

1. Fresh client launch; open Developer Toolkit → Textures.
2. Press `TRACE DXT5` once, wait for deferred processing, then press display-only `Native Trace Status` and screenshot status plus preview.
3. Press `TRACE BGRA8` once, wait, then press `Native Trace Status` and screenshot status plus preview.

For each generation record GPU CORRELATION, constructor/backend HIT, resource, descriptor, descriptor-resource snapshot/match, both thread IDs, dimensions and selectors. Expected unchanged visuals: DXT5 correct quadrants; BGRA8 corrupted stripes. `Native Trace Status` must not issue a request. GPU FORMAT and LOCKRECT PITCH must remain UNKNOWN. Any crash, visual change, AMBIGUOUS state, resource mismatch, unexpected selector/dimension, or missing expected request/parser baseline is a failed experiment: stop and restore the prior DLL. Runtime descriptor correlation becomes VERIFIED_RUNTIME only after both controlled traces show exact resource→descriptor→backend equality without ambiguity.

## Known-good baseline

Owner-reported stock tests: Palette, DXT1, DXT3, DXT5 visible; separate offline BGRA8→DXT5 file visible with red/blue/yellow/cyan quadrants. `TBCExtensions 0.1.7-blp2-native-correlation-research`, API 7, profile `MASTERWOW_CUSTOM_8606` are **current source identifiers**; no owner result establishes that the exact repository DLL SHA is the installed known-good binary. Precise known-good binary identity: **UNKNOWN**. Keep the owner's existing working executable/DLL copies. The 0.1.6 all-controls-blank incident was an archive lock, **not** a verified format regression.

Use separate clean client launches for trace comparisons and record client/DLL version, asset path, archive availability, button presses, complete Toolkit text, visual preview, and `TBCExtensions.log`. `TRACE DXT5` and `TRACE BGRA8` in `../MasterWoWDevTools/Modules/TextureBrowser.lua` clear preview, arm the one-shot native trace, request the controlled asset through stock `Texture:SetTexture`, and show status. `Native Trace Status` refreshes status. The trace reports HIT only when request path matches and deep calls carry its generation; a global total increase is not a matched deep hit. `return 1` at `0x00457FC0` is a non-null boolean. `BLP2 UNKNOWN` means header bytes were not correlated, not invalid BLP data.

Latest owner comparison: both tests matched one request at `0x00457FC0` on thread 30304 with raw pointer `0x27E8AC08`, but both had zero deep equality candidates and zero correlated source/parser hits. Fresh-client original BGRA8 visibly produced a distinct corrupted horizontal/pixel pattern, while the offline DXT5 copy showed the expected clean quadrants. The fresh BGRA8-only control is **complete**; do not repeat it merely to resolve stale-preview ambiguity. Visible BGRA8 is not a correct format PASS. Do not alter file bytes or enable transform.

Next research: static identification of a distinguishing retained resource/path field and post-parse format-index boundary. Only then propose a narrow qualified observation. Last common stage and first divergence must be derived from matched traces, not global totals. User performs client runtime tests; build/static checks do not prove in-game behavior.

The compiled observation-v2 DLL adds `0x00455EF0 object-identity CANDIDATE: N pointer-equal calls` and `object 0x...`; the owner has tested it. Both diagnostics remain read-only and are **not** controlled deep HIT evidence. Do not infer byte ownership or parser reachability from the numerical pointer.

Future real 3.3.5a BLP matrix (not started): byte-identical UI artwork, icon, palette, DXT1/3/5, alpha, multi-mip, non-square, larger, and uncompressed variants if present. Record actual BLP encoding/features and classify each **STOCK TBC WORKS**, **TBCExtensions compatibility required**, **UNSUPPORTED**, or **UNKNOWN**. An expansion label alone does not classify a file.

## Resource-name observation revision — manual test pending

The newly compiled observation DLL uses the **same three existing pass-through detours**. Its Toolkit text is intended to fit one screenshot: REQUEST path/generation/thread; RESOURCE pointer, bounded inline `+0x0C` name, path match and `CACHE UNKNOWN`; SOURCE/PARSER path-associated candidate counters and threads; readable BLP2 header metadata; and two observed stock format-index fields or UNKNOWN. A path candidate is not unique request proof, and no visual PASS/FAIL is assigned by native code.

Use **two clean 8606 client launches** with the patch/archive closed and the new DLL beside the exact client executable. In launch 1, open Developer Toolkit → Textures, press `TRACE BGRA8` **once**, wait for the preview to settle, then press `Native Trace Status`; screenshot all text and the visual preview. Exit the client. In launch 2, press `TRACE DXT5` **once**, wait, refresh status, and screenshot. Do not press any other texture control in either launch before the trace. Send both screenshots and the DLL/client version or installed DLL SHA if available. Expected *labels* are `REQUEST HIT`, `RESOURCE name +0x0C: ...` or `UNKNOWN`, `SOURCE PATH CANDIDATE` or `NO MATCH`, `PARSER PATH-CALL HIT` or `NO HIT`, and `FORMAT indices OBSERVED` or `UNKNOWN`; no downstream HIT or format value is promised. Runtime replacement must remain DISABLED.

## 2026-09-16 first-versus-second lifecycle test

The owner has already obtained parser-correlated traces for **both** controls; the old “manual test pending/no deep correlation” descriptions above are historical, not current conclusions. `TRACE BGRA8` and `TRACE DXT5` each clear the preview, arm a generation, and issue **one** controlled stock request. `Native Trace Status` only displays accumulated state and does **not** load a texture. The trace button also displays status immediately after its single request, so TRACE → Native Trace Status is one request plus two displays.

Test A: fresh 8606 client, patch/archive closed, do not preview BLP2Test first. Open Toolkit → Textures; press `TRACE BGRA8` once, then `Native Trace Status`, screenshot generation 1 and preview. Without restarting, press `TRACE BGRA8` once more, then `Native Trace Status`, screenshot generation 2. Test B: completely close/restart the client; do not preview OfflineDXT5 first; repeat the same first/second sequence with `TRACE DXT5`. Compare request/name matches, resource pointers, `0x00455EF0` PATH CANDIDATE, `0x005B1BA0` PATH-CALL HIT, BLP metadata, width/height, and the two internal selectors. The current Toolkit cannot prove a per-generation cache hit or miss; expect **CACHE UNKNOWN**. Do not infer a cache hit merely from a parser NO HIT. Runtime replacement must say DISABLED.

**Owner completed this first/second test:** For both controls, immediate TRACE status can show request/name HIT while source/parser are still NO HIT. On the **same generation**, display-only Native Trace Status later shows source PATH CANDIDATE and parser PATH-CALL HIT. This repeated on second generations. BGRA8 remains visibly corrupted (encoding 3, alpha 8/0, first selector 5); DXT5 remains correct (encoding 2, alpha 8/7, first selector 7). This does not prove cache hit/miss. The new DLL formats resource dimensions, selector #1 and selector #2 on separate lines to avoid right-edge clipping. If testing the latest DLL, press one TRACE button, screenshot its immediate text, then press Native Trace Status without clicking another texture control and screenshot the **same generation** again. Record both selector lines, thread IDs and visual state. Do not treat the status press as another load; runtime replacement must stay DISABLED.
## Controlled GPU/upload observation test (latest DLL)

Install `build-x86-vs18/bin/Release/TBCExtensions.dll` (SHA-256 `81D2C45D9CA6DCCFE6F9577B9E31CCFD18A591D0BF0209B98DDF6211EC0AE562`) beside the matching build-8606 client. Test DXT5 first: press `TRACE DXT5` once, wait for the preview, then press display-only `Native Trace Status` and screenshot the full status plus preview. Then press `TRACE BGRA8` once, wait, press `Native Trace Status`, and screenshot. Do not use Preview/control buttons between arm and status. Expected stock visuals remain DXT5 correct quadrants and BGRA8 stripes. Report CREATE selector→accepted selector, GPU FORMAT, texture pointer, UPLOAD/COPY hits and threads, mip-0 source/destination pitches, copy classes, rows and DERIVED STATIC bytes. Attempts/HRESULT may correctly remain UNKNOWN. Stop and restore the prior DLL on crash, AMBIGUOUS, resource mismatch, missing baseline, or any visual change. Native Trace Status must not issue a request; replacement must remain DISABLED.
**Owner completed the GPU/upload test:** DXT5 reported accepted selector 7, DXT5, Pitch 256/256, class 7/7 and 4,096 derived bytes with correct visual. BGRA8 reported accepted selector 5, DXT1, Pitch 128/128, class 5/5 and 2,048 derived bytes with striped visual. These values are VERIFIED_RUNTIME. No further repetition is required except for the separately armed controlled 1/2 prototype below. Runtime transcode remains DISABLED.

## Controlled native BGRA8 1/2 prototype test

Use corrected DLL SHA-256 `B801058E392D0DBD28189FB2C95E8D95A4247659C51A7304F757DF404C257DAC`. Back up the installed DLL, copy only this DLL, and start a fresh matching build-8606 client with patch/archive tools closed. Open Developer Toolkit → Textures. **Do not press stock BGRA8, Preview, or any BLP2Test control first.** Press `PROTO BGRA8` exactly once, wait for the preview, then press display-only `Native Trace Status`. Screenshot the entire preview and status.

Success requires: prototype APPLIED; exact resource/path; BLP2 3/8/0 and 64x64; original selectors 5/0; compatibility selectors 1/2; verified GPU correlation; creation selector 1 accepted as 1; A8R8G8B8 format 21; upload/copy HIT; source and locked Pitch 256; class 1/1; 64 rows; 16,384 DERIVED STATIC bytes; and the intended image rendered correctly. ARMED without APPLIED usually means the resource was already cached or no fresh constructor occurred; REJECTED means stop and report the reject code/status. Native Trace Status must not issue another request.

The prior generation-1 result `REJECTED 5` was expected fail-closed behavior caused by a corrected state-order bug; it did not test 1/2. New status reports `PROTOTYPE REJECT: number (symbol)` plus gate counter/result snapshots. Do not reuse the prior DLL.

After a successful prototype trace, press `TRACE DXT5` and confirm the existing 7/7 DXT5 path still renders correctly. To verify the unmodified stock failure, fully restart the client, do not arm the prototype, press `TRACE BGRA8`, and confirm 5/0, DXT1, Pitch 128, class 5/5, 2,048 derived bytes and stripes. Do not run stock BGRA8 before the prototype in the prototype session. Runtime transcode remains DISABLED.

**Owner result for this artifact:** The prototype applied resource 5/0→1/2, but descriptor creation had already snapshotted 5/0 upstream. Runtime therefore showed resource 1/2 and source Pitch 256 together with descriptor/copy 5/5, DXT1, locked Pitch 128, and a changed corrupted preview. This is a valid research result, not a successful full 1/2 render. Do not repeat this DLL expecting A8R8G8B8, and do not promote it to native compatibility. A later build must move the exact controlled correction to the verified pretranslation `0x00454F40` boundary; no such build exists yet.

## Coherent 0x00454F40 classification prototype

Use DLL SHA-256 `7DBD87CD686F13B023D7135EFA19B27BC7774A4D46C2EC53E5C26A7FACD51CBC`. Session A: start a fresh build-8606 client and do not preview or trace BLP2Test first. Open Developer Toolkit → Textures, press `PROTO BGRA8` exactly once, wait, then press display-only `Native Trace Status`. Capture the preview and all status lines. Required trace: prototype APPLIED; CLASSIFY HIT; incoming 5/0 and forwarded 1/2; resource original 5/0 and compatible 1/2; exact GPU correlation; descriptor/create selector 1 accepted 1; A8R8G8B8 (`0x00000015`); upload/copy HIT; class 1/1; source Pitch 256; 64 rows; 16,384 DERIVED STATIC bytes; correct visual. Locked destination Pitch may be 256 or larger and is not an equality gate.

Session B: fresh client, press `TRACE DXT5`, wait, then Native Trace Status. Require unchanged 7/7, DXT5, source Pitch 256, class 7/7, 4,096 derived bytes and correct quadrants. Session C: fresh client, press stock `TRACE BGRA8`, wait, then status. Require unchanged 5/0, DXT1, class 5/5 and corrupted stripes. Do not combine the prototype and stock-BGRA control in one cached session. Stop and restore the previous DLL on REJECTED/AMBIGUOUS, missing exact correlation, unexpected selectors, crash, or changes to either stock control.

**Owner result:** Session A passed completely, including correct pixels; session B passed unchanged; session C confirmed the classifier was not armed and resource selectors stayed 5/0. The exact control is now `CONTROLLED_NATIVE_BGRA8_1_2_VERIFIED_RUNTIME`.

## Real-asset intake and lifetime tests

Place candidate files for offline review under `BLP2Research/real-assets/` in category subfolders; do not overwrite the verified samples or client assets. Run `build-x86-vs18/bin/Release/blp_classify.exe BLP2Research/real-assets`. A `NATIVE_BGRA_1_2_CANDIDATE` result is only intake approval. Add a new exact path/dimension/mip-count allowlist entry and independent Toolkit control in a later task before client loading.

For each newly allowlisted asset use a fresh client and one explicit control. Capture every `MIP n DETAIL` line and verify halved dimensions, source Pitch `width*4`, class 1/1, actual locked Pitch, row count equal to mip height, and derived bytes `width*height*4`. After initial success only, test UI Clear/reload, repeated SetTexture, destruction/recreation of the UI Texture object, and cache reuse. A normal video-mode apply may be used for a device recreation test if the owner's client already supports it; do not inject or force device loss. The invariant is persistent resource 1/2, descriptor/recreated texture 1/1/A8R8G8B8, and class-1 upload.
